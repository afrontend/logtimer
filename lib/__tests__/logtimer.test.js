const assert = require("assert");
const logtimer = require("../index.js");

describe("logtimer", () => {
  it("has a test", () => {
    assert(logtimer !== undefined, "logtimer should have a test");
  });
});
